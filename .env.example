# iFilter Socket Server — Environment Variables
# Copy this file to .env and fill in your values

# Port the socket server listens on
PORT=3001

# Must match the JWT secret in iFilterDashboard-BackEnd/api/core/AuthMiddleware.php
JWT_SECRET=iFilter_Secret_Key_2025

# Comma-separated list of allowed CORS origins
ALLOWED_ORIGINS=http://localhost:5173,https://ikosher.me
